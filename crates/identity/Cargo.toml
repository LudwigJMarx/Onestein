[package]
name = "onestein-identity"
version = "0.0.0"
description = "Identities, devices and their identifiers"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
onestein-crypto = { path = "../crypto", version = "0.0.0" }

[lints]
workspace = true
