[package]
name = "onestein-contact"
version = "0.0.0"
description = "Contact establishment: bundles, links, rendezvous"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
onestein-bdf = { path = "../bdf", version = "0.0.0" }
onestein-crypto = { path = "../crypto", version = "0.0.0" }
x25519-dalek = { version = "3.0.0", features = ["static_secrets"] }

[dev-dependencies]
onestein-bdf = { path = "../bdf", version = "0.0.0" }

[lints]
workspace = true
